application:
  author: ING. Wakeu Georges Favier
  title: Book-Network
  version: 1.4.3

spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
  flyway:
    baseline-on-migrate: true
    enabled: true

springdoc:
  default-produces-media-type: application/json

server:
  error:
    include-binding-errors: always
    include-message: always
  port: 8090
  servlet:
    context-path: /api/v1/
